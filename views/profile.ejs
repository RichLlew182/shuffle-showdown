<!DOCTYPE html>
<html lang="en">
<%- include('partials/head', { title: 'Profile' }) %>

    <body>

        <% if (locals.userProfileData) { %>

            <h1>Welcome <%= userProfileData.display_name %>!
            </h1>

            <% } else { %>

                <h1>No User Found!</h1>

                <% } %>



    </body>

</html>